
import React from 'react';
import Logo from '../components/logo'; // Ajuste o caminho conforme necessário

const Projects: React.FC = () => {
  return (
    <div>
      <Logo /> {/* Aqui você está utilizando o componente Logo */}
      <h1>Página de Projetos</h1>
      {/* Resto do conteúdo da página */}
    </div>
  );
};

export default Projects;
